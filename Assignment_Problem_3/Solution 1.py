# Write a Python program to square the elements of a list using map() function.

list1 = [4,5,2,9]
print(list(map(lambda x : x*x, list1)))

# also can be taken input from the user of the list as i shown in solution 2 but without using lambda and defining separate # function

